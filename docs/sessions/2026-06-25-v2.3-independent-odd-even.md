# 2026-06-25 · v2.3 独立奇偶页裁切

## 背景

v2.2 引入 `--crop manual` 兜底工具，使用 `mirror_even=True` 让偶页自动
inner↔outer 互换。实测 `尸子卷 0021.png` 等古籍扫描：奇偶页版框/鱼尾形态
不对称，镜像错位。

## 决策

- 废弃 `mirror_even` 镜像方案
- 引入双 `PageCropProfile`（odd_page + even_page）
- JSON v1 → v2 自动迁移（用户在内存中升级，旧 preset 文件不动）
- GUI 双 Spinbox + 双拖框按钮（各自独立编辑奇/偶页）

## 变更

- `src/book_cut/detect/manual.py`：
  - 新增 `PageCropProfile` dataclass（4 padding）
  - `ManualCropProfile` 字段替换：`odd_page` + `even_page`
  - `apply_manual_crop` 简化（按 `is_even` 选 profile）
  - `padding_from_rect` 返回 `PageCropProfile`，移除 `mirror_even` 参数
  - `from_json` 兼容 v1 + v2；`to_json` 输出 v2
- `src/book_cut/cli.py`：
  - 新增 `--manual-even-padding "T,B,I,O"`
  - `--manual-mirror-even` 改 default=None + 加废弃说明
- `src/book_cut/pipeline/orchestrator.py`：
  - 构造 `ManualCropProfile(odd_page=..., even_page=...)`
  - 偶页未指定时从奇页镜像生成（保持 v1 行为兼容）
  - `--manual-mirror-even` 传值时 warn 忽略
- `src/book_cut/gui.py`：
  - Manual Crop 面板拆两行（奇页 / 偶页）+ 4+4 个 Spinbox
  - "📐 拖奇页框" / "📐 拖偶页框" 两个独立按钮
  - 删除偶页镜像 checkbox（语义废弃）
  - `apply_profile_to_vars` 加 `is_even` 参数
- `src/book_cut/gui_canvas.py`：
  - 移除 `_mirror_even` 实例变量
  - `get_profile` 用 `_profile_snapshot` 保留另一侧 profile
- `samples/crop_profiles/v2_独立奇偶示例_尸子卷.json`：
  - 新增 v2 示例 preset（独立奇偶 padding）

## 验证

- `pytest tests/test_manual_crop.py -v` → 32 通过（v2.3+ 测试集）
- `pytest tests/ -v` → 422 通过，无回归
- v1 preset（`尸子卷_浙江书局_光绪三年刊.json`）加载正常，自动迁移到 v2
- v2 preset round-trip 一致

## 后续

- 真样本跑：尸子卷 0021.png 重新拖框 odd/even，对比 v2.2 镜像效果
- 用户视角：GUI 双拖框按钮各拖各的，独立保存到 preset