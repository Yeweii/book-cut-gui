# 2026-06-22 · book-cut v0.1.8 release — v1.7 打包

> 续 `2026-06-22-book-cut-v1.7.md`（PDF 页面统一尺寸）。
> 本轮把 v1.7 的代码状态落到可发布的产物：macOS .app。

## 概要

| 产物 | 大小 | 验证 |
|---|---|---|
| `dist/Book Cut.app` (v0.1.8) | 234M | arm64，启动无 import 错误，PYZ 含 `book_cut.io.page_size` |

CLI 端到端（ZHSY100456 真实样本，66 页）：
```bash
$ book-cut --input samples/ZHSY100456_*.pdf --output /tmp/book-cut-v18 \
    --split gutter --crop border --binarize sauvola --pdf \
    --pdf-page-size a4

# 期望：输出 PDF 所有页 = A4 (595.5 × 842.25 pt)
# 验证：python -c "import pypdf; r=pypdf.PdfReader('/tmp/...pdf'); print(set((p.mediabox.width, p.mediabox.height) for p in r.pages))"
# → {(595.5, 842.25)}  # 全是 A4
```

---

## 改动清单

### 版本 bump（3 处）

| 文件 | 旧 | 新 |
|---|---|---|
| `pyproject.toml` | 0.1.7 | 0.1.8 |
| `src/book_cut/__init__.py` (`__version__`) | 0.1.7 | 0.1.8 |
| `packaging/Book Cut.spec` (CFBundle*) | 0.1.7 | 0.1.8 |

### Spec hiddenimports 适配 v1.7

新增（v1.7 新模块）：
```python
"book_cut.io.page_size",  # v1.7：PDF 页面统一尺寸
```

PyInstaller 默认对 dynamic import 的子模块可能漏打包，显式列上避免 .app 启动时 `ModuleNotFoundError`。
其他模块（`io.loader` / `io.exporter` / `pipeline.*`）v1.6 已列，无需重复加。

### CHANGELOG.md

v0.1.8 段在 v0.1.7 段前，已记录在 `c295930` commit 内。

---

## 验证清单

- [x] `pip show book-cut` → Version: 0.1.8
- [x] `pyinstaller --noconfirm --clean "packaging/Book Cut.spec"` 构建成功（19.9s 完成）
- [x] `dist/Book Cut.app/Contents/Info.plist` → CFBundleShortVersionString = 0.1.8
- [x] `dist/Book Cut.app/Contents/Info.plist` → CFBundleVersion = 0.1.8
- [x] `dist/Book Cut.app/Contents/Info.plist` → CFBundleIdentifier = com.bookcut.app
- [x] `file dist/Book Cut.app/Contents/MacOS/Book\ Cut` → Mach-O 64-bit executable arm64
- [x] `.app` 二进制启动 12s 无 import 错误
- [x] Tk loaded：`libtcl9.0.dylib` + `libtcl9tk9.0.dylib` + `_tkinter.cpython-311-darwin.so` 已 mmap
- [x] PYZ 归档含 `book_cut.io.page_size`（pyi-archive_viewer -r 验证）
- [x] 228 测试全过（v1.7 已验，packaging 期间未跑新单测——打 spec 等同 v0.1.7 流程）

---

## 已知遗留（不在本次范围）

- **codesign / notarize**：未做（需 Apple Developer 账号 + 证书）。Gatekeeper 拦截需用户手动 bypass：
  - 右键 → 打开
  - 或 `xattr -d com.apple.quarantine "dist/Book Cut.app"`
- **跨架构**：默认仅 arm64（Apple Silicon）。Intel Mac 需在 Intel 上重跑 `bash packaging/build_macos.sh`
- **PyPI publish**：未做（需 twine + PyPI 账号 + 用户明确放行）。本地 wheel + sdist 用户可手动 `python -m build` 出

---

## commit

```
<待>  build: v0.1.8 打包 — spec 适配 v1.7 page_size
```

---

## 后续（v1.7+ 候选）

- **fit 风格选项**（contain / cover / fill / none）—— task 候选
- **多 PDF 独立 page size** —— task 候选
- **v1.7 分支合 master**：用户拍板（何时合 / 是否要加 release note 在 README）
- **PyPI publish**：用户明确后做（v0.1.8 wheel + sdist 已 ready）
- **codesign + notarize**：有 Apple Developer 账号后做